import { test } from "@playwright/test";

// Esign App Workflow ...
// no permissions user
test.describe("Esign App workflows", () => {
  // how to login ??
  // test disclaimer
  // test all values in esign landing screen
  // test tokens -> 0, 2, anything higher than 10
  // test ability to upload files
  // test ability to select default templates
  // test ability to add new signers, toggle between creator and new signer && edit their values
  // test ability to draw in the screen based on the signer
  // test ability to 'Prepare Esign' and test error messages.
});
